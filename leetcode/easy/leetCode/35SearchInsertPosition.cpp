#include <iostream>
#include <vector>
using namespace std;
int main(int argc, char *argv[]) {
	
}

int searchInsert(vector<int>& nums, int target) {
    int i = 0;
	 while ((target > A[i]) && (i < n)) ++i;
	  
	 
	return i;
}

 